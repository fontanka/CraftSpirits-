# CraftSpirits — автоматика дистилляции и ректификации

Самодельная замена БКУ-07М на базе ESP32 (firmware) + Raspberry Pi 4 (standalone Python-сервис с веб-UI и touchscreen на щите). Цель — повторить функционал заводской автоматики, убрать её ограничения и оставить место для собственных алгоритмов. Без зависимости от Home Assistant — можно ставить «как готовое устройство».

## 1. Целевые режимы (MVP → V1)

| Режим | MVP | V1 |
|---|---|---|
| Дистилляция (потстил), отсечка по T куба | ✅ | + отсечка по ΔT/dt и по объёму |
| Ректификация старт-стоп с клапаном | ✅ | + автокалибровка пауз стабилизации |
| ШИМ-отбор (пропорциональный) | — | ✅ |
| Управление дефлегматором (bang-bang клапан воды + ШИМ-отбор; cascade PID опц.) | bang-bang | + cascade PID если потребуется |
| Барокоррекция T кипения по BME280 | — | ✅ |
| Аварийная отсечка (soft+hard) | ✅ | ✅ |
| Логирование + графики в HA | ✅ | + экспорт CSV рецепта |

## 2. Архитектура

**Standalone-приложение, без зависимости от Home Assistant.** Brain в виде Python-сервиса на Raspberry Pi 4, ESP32 — «глупая» периферия с safety, локальный 7" touchscreen на щите + доступ с телефона по LAN.

```
   ┌── DS18B20 × 4–6 (1-Wire)
   │
[ESP32-S3]──ESPHome──── Wi-Fi / native API ───┐
   │                                           │
   ├── SSR ТЭНа (slow PWM)                     │
   ├── SSR клапан отбора                       │
   ├── SSR клапан воды                         │
   ├── Реле enable-контактора                  │
   └── E-stop / биметалл / термопред (GPIO in) │
                                               │
                       ┌───────────────────────┴───────────────┐
                       │  Raspberry Pi 4 (4 GB) + USB-SSD       │
                       │                                        │
                       │  CraftSpirits Controller (Python)      │
                       │  ├── FastAPI + WebSocket               │
                       │  ├── aioesphomeapi → ESP32             │
                       │  ├── state machines (PotStill,         │
                       │  │       RefluxStartStop, ...)         │
                       │  ├── SQLite (сессии, рецепты, события) │
                       │  └── Telegram alerts (опц.)            │
                       │                                        │
                       │  Web UI (Svelte + Chart.js)            │
                       │  http://still.local:8000               │
                       │       ▲                                │
                       │       │                                │
                       │  Chromium kiosk → localhost:8000       │
                       │       │                                │
                       └───────┼────────────────────────────────┘
                               │ HDMI + touch
                       ┌───────┴──────────┐
                       │ 7" Touch Display │  ← на дверце щита
                       └──────────────────┘
```

**Разделение ответственности — критично для безопасности:**

- **ESP32 (ESPHome)** — единственный, кто физически управляет SSR/реле. Знает только примитивы: «выставь мощность ТЭНа N%», «открой клапан». Имеет независимые safety-инварианты, которые отрубают железо при их нарушении даже без команды сверху.
- **Raspberry Pi 4 (CraftSpirits Controller)** — высокоуровневая логика режимов, рецепты, история, UI. Падение Pi не должно приводить к небезопасному состоянию: ESP должен сам уйти в safe state по watchdog (нет команды > 30 сек).
- **Touchscreen** — это просто браузер, открытый на `localhost:8000`. Падение Chromium не влияет на ничего, кроме UI.

**Почему не Home Assistant**: standalone делает систему независимой от HA-обновлений, упрощает развёртывание (один Docker образ или systemd-юнит), позволяет упаковать как готовый продукт. Минус — теряем «бесплатные» интеграции HA (но Telegram и базовый веб-доступ делаем сами).

**Почему именно ESPHome на ESP32** (а не своя прошивка): YAML-конфиг вместо C++, OTA из коробки, отлаженные драйверы DS18B20/BME280/slow-PWM, официальный Python-клиент `aioesphomeapi` для разговора с Pi.

## 3. BOM (Bill of Materials)

> **Зафиксированы решения:** ESP32-S3-DevKitC-1, ТЭН 3–5 кВт, охлаждение дефлегматора — водопроводная вода через соленоидный клапан 230В (без помпы). **Сборка без пайки** — все модули заказываем готовыми, соединения через винтовые клеммы, Wago 221 и обжимные гильзы НШВИ.

### Стратегия «без пайки»

Заменяем пайку на три механизма:
- **Винтовые клеммы** на каждом модуле (выбираем модули с встроенными)
- **Wago 221** — рычажные клеммники для 230В соединений и распределения шин датчиков
- **НШВИ + кримпер** — оконцовка многожильного провода в винтовые клеммы (5 сек на провод)
- **DuPont F-F джамперы** — сигнальные соединения между модулями и ESP

Единственный возможный риск: если попадётся BME280 без впаянных штырей. В BOM явно указано «assembled» — проверяйте перед заказом.

### Управление и логика

| Компонент | Модель / параметры | Кол-во | Примечание |
|---|---|---|---|
| Brain | **Raspberry Pi 4 (4 GB) + блок питания 5В 3А USB-C** + корпус с пассивным радиатором | 1 | Запускает CraftSpirits Controller (Python). 4 GB достаточно с большим запасом. |
| Хранилище Pi | **USB 3.0 SSD 64–128 GB** (Samsung T7, Kingston DT80, Crucial X6) + USB-C ↔ USB-A кабель | 1 | Не SD-карта: SSD надёжнее в ~10× для 24/7 работы. SD карту можно как fallback recovery. |
| ОС-карта (резерв) | microSD 32 GB Class 10, A2 | 1 | Для первой установки и как backup |
| Touchscreen | **Raspberry Pi 7" Touch Display 2** (официальный, 720×1280) или 7"/10" HDMI+USB-touch с поддержкой Linux | 1 | Официальный подключается через DSI + питается от Pi. HDMI-варианты тоже работают. |
| Крепление touchscreen | Корпус-рамка с креплением на дверцу DIN-щита или отдельная VESA-крепёжка | 1 | Подбирается под щит. |
| Контроллер ESP | ESP32-S3-DevKitC-1, любая из вариаций N8R2/N8R8/N16R8 | 1 | **С заводски припаянными** штырьковыми гребёнками. Используемые GPIO безопасны на всех вариантах. |
| Screw shield для ESP | Expansion board под ESP32-S3-DevKitC-1, **44-pin** (не 38-pin от классического ESP32) с винтовыми клеммами на все GPIO | 1 | Поиск AliExpress: `ESP32-S3 DevKitC-1 expansion board screw terminal`. После него все GPIO — клеммы. |
| БП логики (ESP+реле) | **Mean Well IRM-15-5** (5В 3А DIN) | 1 | DIN-rail, винтовые клеммы, изолированный. Pi питается отдельно от своего БП через USB-C. |
| Wi-Fi роутер | Любой существующий в доме | — | ESP32 ↔ Pi общаются по Wi-Fi (или Ethernet, если повезло с проводкой). Без интернета работают, только LAN. |
| LDO 3.3В | На борту DevKitC-1 (AMS1117) | — | Хватает для ESP + DS18B20 + BME280. |
| Корпус | DIN-щит наружного монтажа на 12–18 модулей, IP54, **с прозрачной/металлической дверцей под touchscreen** | 1 | Перфорация под кабель датчиков. Дверца с вырезом под 7" экран — можно проще: смонтировать дверцу с большим окном и закрепить дисплей изнутри. |

### Силовая часть (под 3–5 кВт, однофазный ТЭН)

| Компонент | Модель / параметры | Кол-во | Примечание |
|---|---|---|---|
| SSR для ТЭНа | **Crydom D2425 / D2440 / Sensata CWA2425** (24 А / 40 А, оригинал у Mouser/Digi-Key/Farnell) ИЛИ Fotek SSR-80DA с **гарантированным двойным запасом** + **большой радиатор** + термопаста | 1 | **ВНИМАНИЕ**: на AliExpress 80–90% «Fotek SSR-40DA» — подделка с реальным триаком 12–16 А вместо 40 А. Документированные пожары у homebrewers. См. секцию 12.10. Минимум: брать SSR-80 на нагрузку 22 А (двойной запас) и тестировать тепловой режим. У SSR винтовые клеммы с обеих сторон. |
| Снаббер RC | 0.1 мкФ X2 250В + 100 Ом 2Вт, **с проволочными выводами** | 1 | Под винт прямо на клеммах SSR (не на плате). |
| SSR для клапана отбора | **Omron G3MB-202P breakout, ASSEMBLED** (SSR уже впаян в плату с винт.клеммами 230В и пин-хедерами 3.3В) | 1 | Запрос AliExpress: `G3MB-202P module 5V assembled`. Избегать «kit»-версий без распайки. |
| SSR для клапана воды | **Omron G3MB-202P breakout, ASSEMBLED** | 1 | То же. |
| Водяной клапан | Соленоидный 1/2" NC, **230В AC**, медный/латунный, для холодной воды | 1 | Косичка или клеммы → Wago → силовая шина. |
| Реле «enable» контактора | Релейный модуль 1ch, 5В катушка, **3.3V-совместимый low-level trigger**, в сборе с винт.клеммами | 1 | Стандартный синий модуль уже в сборе. |
| Силовой контактор | Модульный AC, **катушка 230В AC**, 2NO, ≥25А | 1 | ABB ESB25-40N-06 или Schneider iCT25 или IEK КМИ-22510. |
| Автомат | C25 (под 5 кВт) или C16 (под 3 кВт) | 1 | Отдельная линия от щита. |
| УЗО | 30 мА AC, ≥25А | 1 | Перед автоматом. |
| Кабель силовой | ВВГнг 3×2.5 (3 кВт) или 3×4 (5 кВт) | по длине | Медь. |
| Предохранитель быстрый | Керамический 16А (3 кВт) / 25А (5 кВт) | 1 | Между SSR и ТЭНом. |

### Датчики и периферия

| Компонент | Модель / параметры | Кол-во | Примечание |
|---|---|---|---|
| Температурные | DS18B20, водозащищённые в гильзе, кабель 1–2 м | 4–6 | Уже с проводами, без пайки. Концы — НШВИ → винт.клемма screw shield. |
| BME280 | **Breakout с пред-распаянными штырьками**, 3.3В I²C | 1 | Запрос AliExpress: `BME280 module pre-soldered 4 pin`. К ESP через DuPont F-F. |
| 1-Wire shared bus | Wago 221-413 × 3 шт (для +3.3В, GND, DQ) | 3 | Альтернатива: hub-плата `DS18B20 multi sensor hub screw terminal`. |
| Резистор подтяжки 1-Wire | 4.7 кΩ 1/4 Вт | 1 | Втыкается прямо в винт.клеммы между +3.3В и DQ. |
| Кнопка E-stop | Грибок NC, фиксируемый, 22 мм, **16А** | 1 | Силовой контакт в разрыв катушки контактора + второй контакт NC на GPIO. |
| Биметалл-аварийник | KSD9700 110°C NC | 2 | На корпус куба, термопаста. В разрыв катушки контактора (силовой) + сигнал на GPIO. |
| Термопредохранитель | 130°C или 140°C NC, керамический | 2 | Последний рубеж, одноразовый. |
| Пищалка | Активная 3.3–5В | 1 | На GPIO напрямую, без пайки (с проводами уже). |
| Реле тока (контроль ТЭНа) | Опционально: бесконтактный датчик тока SCT-013-030 или ACS712-30A | 1 | Для контроля, что SSR не «залип». См. секцию 7. |

## Инструменты для сборки

| Инструмент | Зачем | Бюджет |
|---|---|---|
| Кримпер для НШВИ | Опрессовка концов многожильного провода. Knipex 97 53 04 / Engineer PA-21 / квадратный с AliExpress | $15–50 |
| Набор гильз НШВИ | 0.5 / 0.75 / 1.5 / 2.5 / 4.0 мм² (1.5 мм² — основные для сигнала, 4 мм² — для силы) | $10 |
| Wago 221 ассортимент | 221-412 (×2), 221-413 (×3), 221-415 (×5). Набор 40–60 шт. | $15 |
| Стриппер автоматический | WEICON / Knipex 12 42 195 / любой с автоподстройкой | $20–60 |
| Набор DuPont F-F | 40 шт., 20 см | $5 |
| Мультиметр | Прозвонка + проверка перед подачей 230В | $25 |
| Отвёртка PH1/PZ1 + плоская 2.5мм | Под все винт.клеммы | — |

### Датчики (большая часть уже есть от БКУ)

| Компонент | Модель | Кол-во | Куда |
|---|---|---|---|
| Цифровой термодатчик | DS18B20, герметичный IP67, гильза | 4–6 | Куб, низ царги, верх царги/ТСА, дефлегматор, опц. вода-вход, вода-выход |
| Атм. давление + влажность | BME280 (I²C, 3.3В) | 1 | Барокоррекция T кипения. |
| Подтяжка 1-Wire | 4.7 кОм, 1/4Вт | 1 | Линия DS18B20. |
| Кабель датчиков | МГТФ или экранированный 2×0.5 | по длине | Длинные ветки лучше через USB-C/RJ-разъёмы для замены. |

### Аппаратная безопасность (КРИТИЧНО — не экономить)

| Компонент | Модель | Кол-во | Назначение |
|---|---|---|---|
| Термопредохранитель | биметалл 130–140°C, NC | 1 | **Последовательно с катушкой контактора ТЭНа**. Срабатывает физически независимо от ESP. |
| Силовой контактор | модульный AC 230В, 25А, NO | 1 | Включается реле «enable» с ESP, разрывается термопредохранителем. Без него ТЭН не запитан в принципе. |
| Дублирующий датчик аварии | биметаллический switch 110°C | 1 | На кубе, в линию enable-контактора. |
| Звуковой сигнал | пьезопищалка 5В, активный | 1 | Аварии / завершение режима. |
| Кнопка аварийной остановки | грибок, NC, фиксируемая | 1 | В линию enable-контактора. Физически разрывает силу. |

**Принцип hardware safety:** ТЭН питается через цепочку `автомат → УЗО → E-stop (NC) → биметалл куба (NC) → термопредохранитель (NC) → катушка контактора → SSR → ТЭН`. Любой разрыв в цепи NC = ТЭН обесточен, и софт тут не участвует.

## 4. Распиновка ESP32-S3-DevKitC-1

Избегаем: strapping pins (GPIO 0, 3, 45, 46), USB-OTG (19, 20), UART0 to USB-serial (43, 44), reserved для octal PSRAM (33–37 на варианте N16R8). Все выбранные GPIO безопасны на любой вариации DevKitC-1.

| Функция | GPIO | Тип | Примечание |
|---|---|---|---|
| SSR ТЭНа (slow PWM) | 6 | output (LEDC) | Период 2–5 сек, 0–100% мощность по полупериодам |
| SSR клапана отбора | 7 | output | High = открыт. Прямое 3.3В на вход Omron G3MB. |
| SSR клапана воды | 10 | output | High = вода идёт в дефлегматор. |
| Реле enable-контактора | 12 | output | High = подаёт 5В на катушку реле, оно даёт 230В на катушку контактора |
| 1-Wire (DS18B20 × 4–6) | 4 | input/output | Подтяжка 4.7 кОм к 3.3В |
| BME280 SDA | 8 | I²C | 3.3В питание |
| BME280 SCL | 9 | I²C | |
| Кнопка E-stop (статус) | 13 | input, pullup | Чтение состояния. Физический разрыв в силовой цепи — независимо. |
| Биметалл-аварийник (статус) | 14 | input, pullup | Чтение, для логов. Физический разрыв в силовой. |
| Пищалка активная | 15 | output | 5В через NPN-транзистор или прямо 3.3В для совместимых пищалок |
| Статусный LED RGB | 16, 17, 18 | output (PWM) | На DevKitC-1 уже есть встроенный WS2812 на GPIO 48 — можно его использовать вместо |
| Резерв (опц. расходомер) | 5 | input, interrupt | Если решим ставить YF-S401 для счёта объёма отбора |
| Резерв (опц. второй клапан хвостов) | 11 | output | |

## 5. Силовая схема и расчёты

Всё для самостоятельной сборки силового узла + цифры для проверки. Под однофазную сеть 230 В AC, ТЭН резистивный, нагрузка до 5 кВт.

### 5.1 Полная топология

```
[Дом, главный щит]
     │
   УЗО 30 мА AC, ≥25 А
     │
   Автомат C25 (под 5 кВт) или C16 (под ≤3 кВт)
     │
     │  ВВГнг 3×4 (или 3×2.5)
     ▼
┌─────────────────── НАШ ЩИТ (DIN, IP54) ──────────────────────────┐
│                                                                   │
│   L ───┬─────────────────────────────────────────────────         │
│        │                                                          │
│        ├──► Mean Well IRM-15-5 (230AC → 5V DC, для ESP+реле)      │
│        │                                                          │
│        ├──► БП Raspberry Pi 4 (отдельный 5V 3A USB-C PSU)         │
│        │                                                          │
│        │   ┌──── SAFETY CHAIN ──── катушка контактора ────┐       │
│        │   │                                              │       │
│        ├───┤ E-stop NC → KSD9700 NC → KSD9700 NC →       │       │
│        │   │ термопред NC → ESP-enable relay NO →        │       │
│        │   │ катушка KM (~5W, 230VAC)                    │──┐    │
│        │   └──────────────────────────────────────────────┘  │    │
│        │                                                     │    │
│   N ───┤                                                     │    │
│        │                                                     │    │
│   ╔════╪═════════════════════════════════════════════════════╪══╗ │
│   ║  СИЛОВОЙ КОНТАКТОР KM (2NO 25A, катушка 230VAC) ─ замок ║ │
│   ║                                                          ║ │
│   ║  L1 (после KM) ──► предохр 25A ─► SSR Fotek-80DA ─► ТЭН ║ │
│   ║                                          │              ║ │
│   ║                                          └─ R-C снаббер ║ │
│   ║                                                          ║ │
│   ║  L2 (после KM) ──► Omron G3MB ─► соленоид воды 230V     ║ │
│   ╚══════════════════════════════════════════════════════════╝ │
│                                                                   │
│   L (до KM, всегда живая) ──► Omron G3MB ─► клапан БКУ           │
│           (отбор не зависит от контактора — но E-stop его         │
│            всё равно снимет через ESP по watchdog)                │
│                                                                   │
│   PE ──┬─► корпус щита                                            │
│        ├─► корпус ТЭНа                                            │
│        └─► корпус БКУ / дефлегматора                              │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

**Ключевая идея**: силовой контактор — это «общий замок». Когда он отжат, ТЭН и клапан воды физически обесточены, даже если SSR залип в проводящем состоянии. Контактор отжимается:
- Любым размкнутым NC-контактом в safety chain (E-stop, биметалл, термопред)
- Размыканием ESP-enable relay (программная команда от Pi или watchdog ESP)
- Пропаданием 230В в сети (катушка обесточена)

### 5.2 Safety chain — детально

Серийная цепь из NC-контактов аварийников + NO-контакт ESP-enable relay, замыкающая катушку силового контактора:

```
       Линия L (230VAC)
              │
        ┌─────┴─────┐
        │  E-stop   │  ◄── грибок 22мм, NC, фиксируемый
        │   NC      │     (нажат → разрыв)
        └─────┬─────┘
              │
        ┌─────┴──────┐
        │ Биметалл#1 │  ◄── KSD9700 110°C, NC,
        │  на кубе   │     на корпусе куба через термопасту
        │   NC       │     (T > 110°C → разрыв)
        └─────┬──────┘
              │
        ┌─────┴──────┐
        │ Биметалл#2 │  ◄── второй на корпусе SSR-радиатора
        │ на радиат. │     (если перегрев SSR > 110°C → разрыв)
        │   NC       │
        └─────┬──────┘
              │
        ┌─────┴──────┐
        │ Термопред  │  ◄── керамический, 130–140°C, NC, одноразовый
        │   NC       │     рядом с биметаллом #1, «последний рубеж»
        └─────┬──────┘
              │
        ┌─────┴─────┐
        │  ESP-     │  ◄── релейный модуль 1ch, катушка от GPIO 12
        │  enable   │     NO-контакт: замыкается когда ESP активирует
        │  relay NO │     (нет питания / ESP в reset → разомкнут → KM отпал)
        └─────┬─────┘
              │
        ┌─────┴─────┐
        │ Катушка   │  ◄── ABB ESB25-40N-06 / Schneider iCT25 / IEK
        │ контакт.  │     230VAC, ~5W, втягивание ~150мА, удержание ~25мА
        │  KM       │
        └─────┬─────┘
              │
       Нейтраль N
```

**Почему именно так:**
- Все аварийники — **NC** (normally closed): «нет питания на катушке = разомкнуто». Любой обрыв провода или поломка датчика = безопасное состояние. (Если сделать NO — обрыв провода имитирует «всё в норме».)
- ESP-enable relay — **NO** (normally open): «нет питания ESP = разомкнуто». При reset/потере питания/прошивке/любом сбое ESP — катушка обесточена, KM отпадает. Программная команда «всё в норме» = удерживать включённым.
- Контакты NC-аварийников должны выдержать ≥0.5 А, 250 В AC. Стандартные грибок и KSD9700 справляются без проблем (катушка KM тянет всего 0.025–0.15 А).

### 5.3 Расчёты для 3 кВт и 5 кВт

#### Токи и сечения проводов

| Цепь | Мощность | Ток (230 В) | Сечение | Допустимо |
|---|---|---|---|---|
| ТЭН 5 кВт | 5000 Вт | **21.7 А** | ВВГнг 3×4 (Cu) | 32 А в трубе, 38 А открыто |
| ТЭН 3 кВт | 3000 Вт | 13.0 А | ВВГнг 3×2.5 (Cu) | 25 А в трубе |
| Соленоид воды | ~10 Вт | 0.04 А | ПВС 2×0.75 | 6 А |
| Клапан БКУ | ~20 Вт | 0.09 А | ПВС 2×0.75 | 6 А |
| Катушка KM | ~5 Вт | 0.022 А | ПВС 2×0.5 | 3 А |
| БП логики 5В (первичная) | ~15 Вт | 0.07 А | ПВС 2×0.5 | 3 А |
| Pi 4 + дисплей (USB-C) | ~15 Вт | 0.07 А | стандартный USB-C | — |

**Везде кратный запас.** Алюминий не использовать ни в каких силовых цепях этого проекта.

#### Падение напряжения на проводке ТЭНа

ВВГнг 3×4 Cu имеет погонное сопротивление **R ≈ 4.61 мОм/м** на одну жилу. При длине трассы 10 м (туда+обратно = 20 м):

```
V_drop = I × 2L × R/L = 21.7 × 20 × 4.61e-3 = 2.0 В = 0.87 % от 230 В
```

Приемлемо. Если трасса >25 м — пересчитайте; на 30 м это уже 2.5–3 %, что снижает мощность ТЭНа на ~5 %.

#### Тепловыделение SSR + размер радиатора

SSR (тиристорная пара, типичные данные с datasheet Crydom D2425 / Fotek SSR-80DA): падение напряжения при полной проводимости **V_on ≈ 1.5 В**.

```
P_diss = V_on × I
3 кВт:  P_diss = 1.5 × 13.0 = 19.5 Вт
5 кВт:  P_diss = 1.5 × 21.7 = 32.5 Вт
```

Допустимая температура корпуса SSR: до 80 °C (выше — резкое снижение допустимого тока, риск отказа). В щите летом окружающая 35–45 °C ⇒ допустимое превышение:

```
ΔT_max = 80 - 45 = 35 °C
R_th_required = ΔT_max / P_diss
```

| Мощность | P_diss | R_th нужно | Что взять |
|---|---|---|---|
| 3 кВт | 19.5 Вт | ≤ 1.79 K/W | Радиатор 120×80×30 мм (≈1.5 K/W) ✅ |
| 5 кВт | 32.5 Вт | ≤ **1.08 K/W** | Радиатор 150×100×40 мм (≈0.9 K/W) ✅ ИЛИ 100×60×30 мм + вентилятор 40×40 12В |

**Важный вывод**: радиатор 100×60×30 мм без вентилятора при 5 кВт перегреется. Варианты:
1. Купить радиатор покрупнее (рекомендуется): «алюминиевый радиатор 150×100×40 для SSR» на AliExpress ~$10
2. Поставить вентилятор: тихий 40 мм 12В с регулировкой по T (через термостат KSD9700 75°C NO)
3. Ограничить мощность до 4 кВт (тогда 100×60×30 справится)

После сборки **обязательно тест в Phase 3** (см. секцию 12, Тест 5): 30 минут полной нагрузки, замер температуры SSR ИК-термометром или контактным.

#### Снаббер RC — когда нужен

| Сценарий | Снаббер? | Почему |
|---|---|---|
| SSR-80 + ТЭН (резистивная) | **Опционально, рекомендую** | Гасит EMI при коммутации, снижает помехи на 1-Wire и WiFi. R=100 Ω 2 Вт + C=0.1 мкФ X2 250 В AC параллельно ТЭНу, провода как можно короче |
| Omron G3MB + соленоид воды | **Не нужен** | В G3MB-202P встроен MOV и snubber для индуктивных нагрузок |
| Omron G3MB + клапан БКУ | **Не нужен** | Аналогично |
| Реле + катушка контактора | **Желательно** | MOV (варистор) 275 В прямо параллельно катушке контактора — гасит ЭДС самоиндукции при отпускании |

Снаббер для ТЭНа собирается из двух выводных компонентов: плёночный конденсатор 0.1 мкФ X2 + резистор 100 Ом 2 Вт **в последовательной** цепи, всё параллельно ТЭНу. Можно сделать прямо на клеммной колодке через НШВИ. Или купить готовый модуль `RC snubber 0.1uF 100R AC` на AliExpress (~$3).

### 5.4 Контактор — почему модульный и какой

Нужен модульный AC-контактор (на DIN-рейку, тихий — без «щелчков» при включении). Требования:
- **Катушка 230 В AC** (не 24 В DC! Внимательно при заказе)
- **2 NO силовых контакта** (один для ТЭНа, второй для соленоида воды) или 1 NO ≥25 А (соленоид можно через тот же контакт)
- **Ток** ≥25 А для 5 кВт (с запасом), ≥16 А для 3 кВт
- **AC-1 категория** минимум (резистивная нагрузка), AC-3 не нужно

Конкретные модели:
- **ABB ESB25-40N-06** — 25 А, 4NO, катушка 230VAC. Самый «правильный», ~$25
- **Schneider iCT25** — 25 А, 2NO, катушка 230VAC. ~$20
- **IEK КМИ-22510** или **КМ-25** — бюджет, ~$10. Работают, но щёлкают громче

Контактор имеет вспомогательный NO-контакт у некоторых моделей (1NC+1NO) — пригодится для индикации в ESP («контактор реально включился»), но не обязательно.

### 5.5 Заземление PE

Без компромиссов:
- PE-шина в щите → отдельная медная шинка с винтами, не объединять с N
- PE → корпус щита (если металлический)
- PE → **корпус куба** (через болт-«земля» на дне или сбоку)
- PE → корпус БКУ / дефлегматора (если металл)
- PE → корпус Pi 4 (если корпус металлический, например Argon NEO)
- Сечение PE-провода = сечению фазы (4 мм² при 5 кВт)

Проверка после монтажа: мультиметром R < 1 Ом между корпусом ТЭНа и PE-шиной щита. Затем УЗО-тестером (или просто кнопкой TEST на самом УЗО) — должно отрабатывать.

### 5.6 Порядок включения и отключения

Чтобы не «дёргать» SSR:
1. Включение: сначала Pi (boot ~1 мин) → потом ESP подключается к Wi-Fi → отзывается на Pi → Pi даёт команду `enable_contactor` → ESP замыкает enable-relay → KM втягивается → 230 В на SSR → SSR ждёт команду PWM
2. Выключение нормальное: Pi даёт `heater=0, valves=closed, enable=off` → ESP размыкает enable-relay → KM отпускает → пропадает 230 В на ТЭН и клапан воды
3. Аварийное (E-stop, биметалл, термопред, watchdog): KM отпадает мгновенно (≤20 мс = один цикл AC), независимо от состояния SSR. Pi+ESP узнают по входу состояния и записывают событие в БД

## 6. Программный стек

Standalone, два уровня: **ESPHome на ESP32** (firmware) + **CraftSpirits Controller на Pi 4** (Python-сервис с веб-UI).

### 6.1 ESPHome-прошивка (`firmware/esp32-still.yaml`)

Делает только то, что обязано — никакой высокоуровневой логики:

- читает все датчики (DS18B20 + BME280) с периодом 1 с;
- публикует sensor entities через **ESPHome native API** (Pi слушает через `aioesphomeapi`);
- принимает команды: `set_heater_power` (0–100%), `valve_отбора` (on/off), `valve_воды` (on/off), `enable_contactor` (on/off), `beep` (ms);
- ШИМ ТЭНа через **slow PWM** на GPIO → SSR, период 2–5 сек (для термической инерции это правильнее, чем фазовая регулировка);
- **safety automations** (выполняются на самом ESP, **без Pi**):
  - watchdog: если нет валидной команды от Pi > 30 с → heater=0, valve=closed, contactor=off, beep;
  - T куба > `max_kub_temp` (по умолчанию 105°C) → heater=0;
  - T любого критичного датчика == NaN или вне диапазона `(-10, 130)` дольше 5 с → heater=0;
  - E-stop input == low → heater=0, valve=closed, contactor=off;
  - при загрузке: всё в safe state, контактор включается только по явной команде Pi.

### 6.2 CraftSpirits Controller — Python-сервис на Pi (`controller/`)

Один процесс, один Docker-контейнер (или systemd-юнит). Архитектура:

```
controller/
├── pyproject.toml
├── craftspirits/
│   ├── __init__.py
│   ├── main.py              # FastAPI app, lifespan
│   ├── config.py            # pydantic-settings, .env
│   ├── esp/
│   │   ├── client.py        # aioesphomeapi wrapper, reconnect-loop
│   │   └── entities.py      # маппинг sensor/switch ESPHome → внутренние имена
│   ├── modes/
│   │   ├── base.py          # абстрактная Mode (enter/tick/exit, transitions)
│   │   ├── potstill.py      # дистилляция
│   │   ├── reflux_startstop.py
│   │   ├── reflux_pwm.py
│   │   └── dephlegmator_pid.py
│   ├── safety/
│   │   ├── watchdog.py      # дублирует ESP-watchdog с Pi-стороны
│   │   ├── pressure.py      # барокоррекция по BME280
│   │   └── ramp_limiter.py
│   ├── storage/
│   │   ├── db.py            # SQLite через SQLAlchemy 2.0 async
│   │   ├── models.py        # Session, Event, Recipe, Calibration
│   │   └── migrations/      # alembic
│   ├── api/
│   │   ├── rest.py          # FastAPI endpoints для UI
│   │   ├── ws.py            # WebSocket live-stream (T, состояния, фаза)
│   │   └── auth.py          # простая HTTP basic / LAN-only по умолчанию
│   ├── notifications/
│   │   └── telegram.py      # опц., через python-telegram-bot
│   ├── recipes/
│   │   ├── loader.py        # JSON в БД и обратно
│   │   └── builtin/         # дефолтные пресеты
│   └── tests/
│       ├── modes/           # unit-тесты стейт-машин
│       ├── safety/
│       └── fixtures/        # фейковый ESP для интеграционных тестов
```

Ключевые библиотеки:
- **FastAPI** 0.115+ — REST + WebSocket
- **aioesphomeapi** 25+ — официальный клиент ESPHome native API
- **SQLAlchemy** 2.0 async + **aiosqlite** — хранение
- **APScheduler** — таймеры состояний (опционально, или просто `asyncio.sleep`)
- **pydantic-settings** — конфиг через `.env`
- **structlog** — лог в JSON
- **python-telegram-bot** v21 (опц.)

### 6.3 Веб-UI (`ui/`)

Отдельный пакет, билдится в статику, FastAPI отдаёт.

```
ui/
├── package.json             # Svelte 5 + Vite + Chart.js
├── vite.config.js
├── src/
│   ├── App.svelte
│   ├── lib/
│   │   ├── ws.js            # WebSocket-стор с автореконнектом
│   │   ├── api.js           # REST-вызовы
│   │   └── stores/
│   │       └── session.js
│   ├── routes/
│   │   ├── Run.svelte       # экран 1 (главный во время сессии)
│   │   ├── Start.svelte     # экран 2 (выбор режима / старт)
│   │   ├── Tests.svelte     # экран 3 (калибровка, dry-test)
│   │   └── History.svelte   # экран 4 (список прошлых сессий + графики)
│   └── components/
│       ├── BigTempCard.svelte
│       ├── ChartStrip.svelte
│       ├── ValveIndicator.svelte
│       └── BigStopButton.svelte
└── dist/                    # билд, отдаётся FastAPI как static
```

**Почему Svelte**: маленький бандл (~30 KB рантайм), нет виртуального DOM, простой синтаксис, хорошо работает на Pi-touchscreen без тормозов. Альтернативы — Vue 3 или просто Alpine.js + HTMX (ещё проще, но менее интерактивно для графиков).

Touchscreen-режим: Chromium запускается в kiosk-mode при загрузке Pi → `chromium-browser --kiosk --noerrdialogs http://localhost:8000/run`. Указатель мыши прячется, виртуальной клавиатуры нет (для ввода чисел будут стандартные `<input type="number">` со steppers).

### 6.4 Симулятор для разработки (`sim/`)

Простая модель куба+царги (тепловой баланс + уравнение Антуана для бинарной смеси этанол-вода) на Python. **Симулирует ESPHome native API**, чтобы Controller не отличал реальное железо от симулятора. Позволяет разрабатывать стейт-машины и UI на ноутбуке без Pi и ESP.

```
sim/
├── still_model.py           # ODE-решатель: масса, тепло, состав
├── fake_esphome.py          # asyncio-сервер, говорит aioesphomeapi-протокол
├── scenarios/
│   ├── normal_potstill.py
│   ├── sensor_dropout.py
│   ├── stuck_ssr.py
│   └── network_loss.py
└── run.py                   # `python -m sim.run` — слушает на :6053
```

### 6.5 Развёртывание на Pi (`deploy/`)

```
deploy/
├── pi-image/
│   ├── README.md            # как залить базовый Raspberry Pi OS Lite 64-bit
│   ├── first-boot.sh        # cloud-init / first-boot скрипт
│   └── kiosk.service        # systemd для Chromium kiosk
├── controller.service       # systemd для CraftSpirits Controller
├── docker-compose.yml       # альтернатива systemd
└── Dockerfile               # multi-stage: ui build + controller
```

Обновления: `git pull && systemctl restart craftspirits-controller` или `docker compose pull && up -d`. OTA для ESP32 — стандартный ESPHome.

## 7. Алгоритмы режимов

### 7.1 Дистилляция (потстил)

```
INIT → HEAT_UP → DISTILLING → SHUTDOWN
```

- `HEAT_UP`: heater=100%, ждём T_kub ≥ `t_start` (по умолч. 80°C).
- `DISTILLING`: heater=`p_work` (60-80%), клапан всегда открыт. Логируем.
- Переход в `SHUTDOWN`: T_kub ≥ `t_stop` (98-99°C, с барокоррекцией) ИЛИ dT_kub/dt > порога (вода пошла).
- `SHUTDOWN`: heater=0, beep, журнал.

### 7.2 Ректификация старт-стоп

```
INIT → HEAT_UP → STABILIZE_TR → HEADS → BODY → TAILS → SHUTDOWN
                                  ↑___pulse___↓
```

- `HEAT_UP`: heater=100%, до T_low_tsarga > 60°C (пар дошёл).
- `STABILIZE_TR`: heater=`p_work_heads`, клапан закрыт, ждём `t_stable` минут с условием |dT_top/dt| < ε (захлёб закончен, флегма пошла).
- `HEADS`: повторяющийся цикл `open(t_open_heads) → close(t_pause_heads)`, длится либо по объёму (если есть счётчик), либо по T_kub ≥ `t_kub_body`.
- `BODY`: цикл `open(t_open_body) → close(wait_until |dT_top/dt|<ε)`. Главное условие выхода: T_top > `t_top_max` (~78.5°C+коррекция) дольше `t_dwell` секунд.
- `TAILS`: открыть полностью либо в другую ёмкость (опц. второй клапан), до T_kub ≥ `t_kub_stop`.
- `SHUTDOWN`: heater=0, valve=closed.

Все пороги — поля в JSON-рецепте в БД, редактируются из UI, версионируются (каждое изменение — новая ревизия рецепта).

### 7.3 ШИМ-отбор

Альтернатива старт-стопу: клапан открывается на `duty * period`, `duty` пересчитывается каждые N секунд по PID от ΔT(top - top_setpoint). Лучше, когда клапан/насос позволяет плавную регулировку.

### 7.4 Управление дефлегматором (НЕ наивный PID)

**Главный вывод из англоязычных форумов (homedistiller.org, stilldragon.org)**: простой `PID(T_vapor) → reflux_water_flow` ломает колонну в проде. Паттерн отказа:

1. T_vapor растёт выше setpoint
2. PID требует больше охлаждения → клапан воды открывается на 100 %
3. Дефлегматор переохлаждает (subcool) → **весь пар конденсируется**, вверх не уходит ничего
4. На датчике T_vapor температура стабильна (паров нет) → PID думает «всё ок», держит 100 % охлаждение
5. Колонна «заперта», отбор стоит, T_vapor остаётся чуть ниже setpoint вечно → PID слепнет

Это документированная ловушка, регулярно обсуждается на StillDragon и Home Distiller forum.

**Правильные паттерны** (в порядке сложности):

**А. Bang-bang клапан воды (MVP, наш выбор)** — клапан либо открыт, либо закрыт. Поток воды постоянный и достаточный для полной конденсации. Управление ABV — через старт-стоп **клапана отбора** (как в БКУ-07М). Дефлегматор всегда полностью охлаждает, отбор регулируется ШИМом клапана БКУ. Просто, надёжно, проверено десятилетиями работы БКУ.
- В этом случае секция 7.4 нам и не нужна — управление сводится к 7.3 (ШИМ-отбор).

**Б. Cascade PID (V1, опционально)** — две вложенные петли:
1. Внутренняя (быстрая): `PID_inner(T_coolant_out) → ШИМ воды`, период ~5 сек
2. Внешняя (медленная): `PID_outer(T_vapor) → setpoint для T_coolant`, период ~30 сек
3. **Ограничитель**: `T_coolant_setpoint ≥ T_vapor_setpoint - X°C` (где X=5–10), чтобы не уйти в subcool

**В. Линейный сервопривод на дроссельном клапане воды (V2, как у GENIO)** — позволяет константный 96 % ABV. Сервопривод управляется PID но с грубым шагом (5–10 % от хода за один cycle). Дорого и сложно, для рум/виски не нужно.

**Решение для MVP/V1**: пункт А (bang-bang клапан воды + ШИМ-отбор). Пункт Б — только если реально потребуется на конкретной железке после первых сессий. Пункт В — никогда для нашего применения.

PID **остаётся** для slow PWM ТЭНа: целевая мощность задаётся, реальная выдаётся через slow-PWM SSR. Это простой одноконтурный регулятор, без подвохов.

## 8. Безопасность — чек-лист

- [x] Аппаратный термопредохранитель + биметалл-аварийник в силовой цепи (независимо от ESP).
- [x] E-stop разрывает силовую цепь физически.
- [x] Силовой контактор как «общий выключатель», управляется отдельным реле ESP.
- [x] Watchdog в ESPHome: потеря связи с Pi > 30 с = safe state (heater=0, valve=closed, contactor=off).
- [x] Контроль обрыва/КЗ датчика T куба → safe state.
- [x] Гистерезис и rate-limit на изменение мощности (не дёргаем SSR).
- [x] УЗО 30 мА на вводе.
- [x] Гальваническая развязка управляющих реле от ESP (opto-isolated модули).
- [x] При первом запуске после reset — всегда safe state, ручная активация контактора.
- [x] Журналирование всех аварий в БД на Pi с timestamp (таблица `events`).

## 9. Структура репозитория

```
CraftSpirits-/
├── PLAN.md                          (этот файл)
├── README.md                        (после MVP)
├── firmware/
│   ├── esp32-still.yaml             ESPHome конфиг
│   ├── secrets.yaml.example
│   └── packages/
│       ├── safety.yaml              safety automations
│       └── sensors.yaml             датчики
├── controller/                      Python-сервис на Pi
│   ├── pyproject.toml
│   ├── craftspirits/                (см. секцию 6.2 для деталей)
│   └── tests/
├── ui/                              Svelte web-UI (см. секцию 6.3)
│   ├── package.json
│   ├── src/
│   └── dist/                        (билд)
├── sim/                             Симулятор железа (секция 5.4)
│   ├── still_model.py
│   ├── fake_esphome.py
│   └── scenarios/
├── deploy/                          Развёртывание на Pi (секция 5.5)
│   ├── pi-image/
│   ├── controller.service
│   ├── kiosk.service
│   ├── docker-compose.yml
│   └── Dockerfile
├── recipes/                         Готовые пресеты (загружаются в БД при старте)
│   ├── sugar_wash.json
│   ├── grain.json
│   └── default_potstill.json
├── hardware/
│   ├── wiring_diagram.md            ASCII / Mermaid схема силовой
│   ├── pinout.md
│   ├── bom.md                       (= секция 3 этого плана, для быстрого доступа)
│   └── photos/
└── docs/
    ├── safety.md
    ├── calibration.md               как калибровать DS18B20 в кипящей воде
    ├── recipes_format.md
    ├── pi_setup.md                  установка ОС, kiosk-mode, network
    └── ui_screens.md                эскизы UI (= те ASCII, что я давал)
```

## 10. Дорожная карта

### Phase 0 — план (сейчас)
- [x] Этот документ.

### Phase 0.5 — настройка Pi (полдня)
- [ ] Залить Raspberry Pi OS Lite 64-bit на USB-SSD (через `rpi-imager`).
- [ ] Boot order: SSD-first (`raspi-config` → Advanced → Boot order).
- [ ] Wi-Fi/Ethernet, статический IP или mDNS-имя `still.local`.
- [ ] SSH, обновления, базовые пакеты (git, python 3.12, docker — опционально).
- [ ] Опционально: VNC для удалённой настройки до подключения touchscreen.

### Phase 1 — железо на столе (1–2 недели)
- [ ] Собрать минимальный стенд: ESP32-S3 + screw shield + 2× DS18B20 + BME280 + LED.
- [ ] ESPHome конфиг с публикацией T и состояний выходов.
- [ ] Тестовый Python-скрипт на Pi: подключение через `aioesphomeapi`, чтение значений.
- [ ] Калибровка DS18B20 в кипящей воде + ice bath, запись offset в БД.

### Phase 2 — Controller v0 + UI v0 (2 недели)
- [ ] Скелет FastAPI + WebSocket + SQLite.
- [ ] Скелет Svelte UI: один экран `Run`, читает live-данные с ESP через бэк.
- [ ] Системные сервисы (systemd) для controller и chromium kiosk.
- [ ] Боевая подача 5В на ESP, 3.3В на датчики — тест end-to-end на воде.

### Phase 3 — safety + силовая (1 неделя)
- [ ] Собрать силовой узел: контактор, SSR, термопред, биметалл, E-stop.
- [ ] **Dry tests** (см. секцию 13 и план тестов): лампа 100 Вт вместо ТЭНа, LED вместо клапанов.
- [ ] Проверка watchdog'а: отключить Pi → ESP должен сам уйти в safe state.

### Phase 4 — потстил (1 неделя)
- [ ] Реализация `PotstillMode` в Controller.
- [ ] Симулятор + unit-тесты переходов.
- [ ] UI: экран `Start` с выбором режима, pre-flight check.
- [ ] Боевой прогон на воде (без браги), оценка задержек.

### Phase 5 — ректификация старт-стоп (2–3 недели)
- [ ] `RefluxStartStopMode` + UI с фазами.
- [ ] Экран `History` с архивом сессий.
- [ ] Прогон на спирт-сырце.
- [ ] Подбор `t_stable`, `t_open_*`, `t_pause_*` под вашу колонну.

### Phase 6 — улучшения
- [ ] ШИМ-отбор и PID дефлегматора.
- [ ] Барокоррекция по BME280.
- [ ] Telegram-бот: уведомления + базовое управление командами.
- [ ] Touchscreen polish: размер кнопок под палец, тёмная тема ночью.
- [ ] Docker-образ и инструкция «отдай другу винокуру».
- [ ] Опционально — отдельный режим «бражной колонны».

## 11. Открытые вопросы

Решены:
- ✅ ESP32-S3-DevKitC-1
- ✅ ТЭН 3–5 кВт → SSR-80DA + большой радиатор + автомат C25
- ✅ Охлаждение дефлегматора — соленоидный клапан 230В NC (вместо помпы)

Остаются по ходу:
1. **Учёт объёма отбора** — ставим расходомер (YF-S401) или считаем по времени открытия клапана? GPIO 5 зарезервирован под него.
2. **Второй клапан под хвосты** — нужен или сольём в ту же ёмкость и отделим вручную? GPIO 11 зарезервирован.
3. **Резервирование** — ставим UPS на ESP+роутер? (Без него потеря Wi-Fi на минуту = принудительный safe shutdown по watchdog.)

## 12. Подводные камни (свод из forum.homedistiller.ru)

Систематизация типичных проблем из веток форума по автоматике, которые наша архитектура должна покрыть (или явно проигнорировать с обоснованием). Источники — внизу секции.

### 12.1 Силовая и SSR

1. **Пробой тиристорной пары SSR в проводящее состояние** — самая страшная авария. Симптом: SSR залип «на нагрев», PWM игнорируется, ТЭН греет на 100 %, никакая прошивка не спасает. На форуме это упоминается как причина серьёзных инцидентов; авторы Samovar и HelloDistiller специально защищаются от этого.
   - **Наш ответ**: силовой контактор как «общий замок» (см. секцию 5.1). Контактор отжимается по watchdog/E-stop/биметаллу — независимо от SSR.
   - **TODO для V1**: контроль реальной мощности через PZEM-004T и отжим контактора при расхождении заданной vs измеренной >10 % в течение 10 сек (как сделал Samovar).

2. **Перегрев SSR при 5 кВт на маленьком радиаторе** — 32.5 Вт тепла, радиатор 100×60×30 не справляется. Выгорает SSR за часы.
   - **Наш ответ**: радиатор 150×100×40 (5 кВт) или 120×80×30 (3 кВт), обязательный Тест 5 в Phase 3.

3. **Помехи от тиристорной коммутации мешают всему**: WiFi отваливается, DS18B20 даёт CRC errors, OLED-дисплеи дохнут после нескольких сессий, индукционные плиты создают токовые скачки.
   - **Наш ответ**: ферритовое кольцо на проводе 1-Wire (как минимум — на кабеле к выносному датчику), снаббер RC параллельно ТЭНу, экранированная витая пара cat5e+экран для длинных трасс, питание ESP и Pi от изолированных БП (Mean Well IRM + отдельный USB-C PSU). Кабель датчиков НЕ прокладывать параллельно силовому проводу — пересекать под 90°.

### 12.2 DS18B20

4. **Длина кабеля >30 м → искажение фронтов → CRC errors**. У нас все трассы ≤3 м — не критично, но если потянем выносной датчик (например, в шкаф для дрожжей) — учесть.
   - **Наш ответ**: ESPHome автоматически делает retry при CRC ошибке, фильтрация в `dallas` компоненте уже включена. Если потащим длинный кабель — экранированная витая пара + ферритовое кольцо.

5. **Параллельное питание ТЭНа и шины 1-Wire на одной фазе** → провалы напряжения 5В → датчики кратковременно «зануляют» соседа по шине. Один пользователь на форуме рекомендует **электролит 50 мкФ на 5В прямо у первого датчика на шине** — решает проблему.
   - **Наш ответ**: добавить керамику 100 нФ + электролит 47–100 мкФ на первой клемме 1-Wire в щите. Дёшево, эффективно.

6. **DS18B20 умирают без предупреждения** (полностью замолкают на 1-Wire). Часто после нескольких лет, иногда блокируют всю шину.
   - **Наш ответ**: алгоритм safety должен переносить потерю любого ОДНОГО датчика без катастрофы. Различение critical/non-critical:
     - **Critical** (потеря = немедленный safe shutdown): T_kub
     - **Important** (потеря = понижение мощности до 50 % + warning): T_head
     - **Nice-to-have** (потеря = warning в UI, продолжаем): T_cond_in, T_cond_out

7. **Заземление экрана с одной стороны** (на стороне БП, не на стороне ESP) — иначе «земляная петля» сама ловит помехи.

### 12.3 Алгоритмика отбора

8. **Чёткие температурные границы для голов/сердца/хвостов невозможны** — зависят от куба, материала, крепости браги, давления. Что работает в один день — не работает в другой.
   - **Наш ответ**: параметры (T_start, T_stop, ШИМ-паузы) задаются на старте сессии, и должна быть лёгкая возможность подкрутить «на лету» из UI. Не зашивать в код. Сохранять фактические параметры успешной сессии как «пресет».

9. **Барокоррекция критична на длинных сессиях**: за 8–9 часов перегонки погода меняется, T_кипения этанола дрейфует на ±0.5 °C при ΔP ±13 hPa. Без коррекции — ложные срабатывания «конец тела».
   - **Наш ответ**: BME280 + поправка `T_corr = T_meas - k×(P_now - 1013)`, k ≈ 0.037 °C/hPa для этанола. Применять к T_head перед сравнением с порогом.

10. **Опасный паттерн ШИМ-отбора**: пользователи увеличивают паузы до 60–120 сек для лучшей сепарации голов («микро-стабилизация колонны»). Это работает, но если режим сменился (heads → body → tails) а параметры ШИМ не пересбросились — пропуски тела или захлёб.
    - **Наш ответ**: ШИМ-параметры жёстко привязаны к фазе (heads/body/tails), не общие. При смене фазы — explicit reset, явное логирование в БД.

11. **«Залипание» в фазе тела при стабильной T** часами: алгоритм ждёт превышения T_stop, а оно не приходит, потому что мощность недостаточна или мы уже на хвостах но T_head ещё не дошла.
    - **Наш ответ**: timeout на фазу. Если в body >N часов и dT/dt < 0.05 °C/час — алерт оператору в Telegram (не auto-stop). N задаётся в пресете, по умолчанию 6 часов.

12. **Стабилизация перед отбором тела (10–15 мин)** — обязательный шаг, который часто пропускают: после захлопывания клапана нужно дать колонне «успокоиться» с полным флегмированием.
    - **Наш ответ**: в state machine RefluxStartStop явная фаза `STABILIZE` после `HEADS`, перед `BODY`. Длительность параметризуется, default 15 мин.

### 12.4 Клапаны

13. **Соленоидный клапан старт-стопа дребезжит и гудит** при питании <12 В или с тонкой прокладкой. Со временем механика разбивается.
    - **Наш ответ**: клапан БКУ на 230 В AC (как у штатного БКУ-07М), не 12 В DC. Кикдаун катушки гасится встроенным в G3MB-202P MOV.
    - **Совет с форума**: заменить штатные прокладки на силиконовые — клапан тише и долговечнее. Для нашего MVP не делаем, но запомним.

14. **Клапан в горячей зоне отбора работает в нештатном режиме** — нагревается >60 °C, обмотка и уплотнения деградируют.
    - **Наш ответ**: монтировать клапан БКУ **после** дефлегматора, на холодной линии отбора (как в штатной схеме). Не до.

15. **Регулировка отбора через дроссельное отверстие**: на форуме отмечают, что фактический отбор больше расчётного, нужна калибровка диаметра отверстия эмпирически.
    - **Наш ответ**: добавить в первую сессию замер реального отбора (мерным цилиндром за известное время), записать `mL/min при 100% duty` в калибровочную таблицу пресета.

### 12.5 Размещение датчика T_head

16. **Гильза с кривым тепловым контактом меряет температуру стенки, а не пара** — даёт заниженные показания на 0.5–2 °C, особенно если стенка холодная.
    - **Наш ответ**: гильза тонкостенная (≤0.5 мм), полностью погружена в пар (без касания стенки), **заполнена термопастой**, датчик утоплен до дна гильзы. Длина гильзы ≥ 3× диаметр датчика.

17. **Установка термометра не в той точке колонны** — классическая ошибка новичков. T в нижней части колонны не соответствует составу отбора.
    - **Наш ответ**: T_head = на верху колонны, **прямо перед дефлегматором**. T_kub = в стенке куба под уровнем жидкости. Два датчика, не один универсальный. Не подменять физически потом — это нарушит алгоритм.

### 12.6 WiFi / связь

18. **WiFi в ESP отваливается раз в N часов** при работе под нагрузкой — частая жалоба на форуме. ESP делает self-reset, но процесс продолжается «слепо», если оператор не следит.
    - **Наш ответ** (уже в архитектуре): ESP — автономный, физически управляет SSR. Pi — только высокоуровневая логика. Двусторонний watchdog 30 сек.
    - **Дополнительно**: ESPHome автоматически переподключается с backoff. Поставить точку доступа физически в зоне щита (если основной роутер далеко).

19. **ESP entering boot loop** (упомянуто в ветке про ESP32+PZEM): редкие code paths в UI триггерили перезагрузку, лечилось только power off.
    - **Наш ответ**: вся «толстая» логика в Pi-Python (где удобно дебажить), на ESP только примитивы. ESPHome конфиг минимально сложный, без custom C++ компонентов в Phase 1–4.

### 12.7 Захлёб и контроль давления

20. **Захлёб колонны** возникает при превышении мощности и/или недостаточном охлаждении. Давление в кубе медленно растёт за 5–15 минут перед захлёбом — это окно для предотвращения.
    - **Наш ответ для MVP**: косвенный признак — рост ΔT(out_def − in_def) воды дефлегматора (вода нагрелась = поток упал ИЛИ ректификат перегревается). При ΔT >50 °C — warning, при >70 °C — снижение мощности на 30 %.
    - **Наш ответ для V1**: BMP280/BME280 в кубе через тонкую трубку для прямого замера ΔP. **Ограничение**: на форуме отмечают, что измерение давления в малых домашних колоннах <50 мм рт. ст. — на грани шума BMP280 (±1 hPa ≈ 7.5 мм рт. ст.). Реалистично только для больших колонн >2 м.

### 12.8 Эксплуатация и безопасность

21. **Пары спирта невидимы и взрывоопасны** (LEL ~3.3 % об.). Любая искра в зоне пара = пожар.
    - **Наш ответ**: щит автоматики ставим **минимум в 1–1.5 м от куба**, физически разнесён. Все искрящие контакты (катушка KM, реле) внутри корпуса щита IP54. Соленоидный клапан БКУ — заводской с пылевлагозащитой.

22. **Отключение охлаждения = вылет паров в комнату**. Самая частая бытовая авария — закрылся кран, нет потока в дефлегматор.
    - **Наш ответ**: tripwire по T_cond_out: >50 °C → warning + heater_power = 50 %, >65 °C → emergency shutdown + Telegram alert + пищалка. Без расходомера это лучший косвенный способ.

23. **Не отключать установку при разливе/утечке** — искра реле = поджигатель. Цитата из FAQ homedistiller.ru: «не пытайтесь обесточить оборудование на месте аварии при проливе или прорыве паров спирта».
    - **Наш ответ**: E-stop не вырубает щит целиком, а **только цепь катушки контактора** (т.е. снимает 230 В с ТЭНа и клапанов внутри щита, но логика ESP/Pi продолжает работать). Полный обесток установки — только рубильник в **главном щите дома** (вне зоны пара).

24. **Вентиляция помещения** — приток + вытяжка. Без неё пары накапливаются у пола (пары этанола тяжелее воздуха).

### 12.9 Резюме: что обязательно сделать (чек-лист на план)

- [x] Силовой контактор как «общий замок» — секция 5.1
- [x] Радиатор SSR с запасом + тепловой Тест 5 — секции 5.3, 13.2
- [x] Снаббер RC, ферритовое кольцо на 1-Wire — секция 5.3, добавить в 6.1
- [x] Различение critical/non-critical sensors в алгоритме safety — добавить в 7.x и 8
- [x] Барокоррекция через BME280 — уже в 1, 7.x
- [x] T_head после дефлегматора, T_kub в стенке куба — секция 6.1 (placement guide)
- [x] Размещение щита ≥1 м от куба, IP54 — секция 8
- [x] Tripwire по T_cond_out (потеря охлаждения) — секция 7.x
- [x] Параметры ШИМ привязаны к фазе сессии, не глобальные — секция 7.3
- [x] Watchdog двусторонний (ESP↔Pi) — секции 2, 6.1
- [x] Электролит 47–100 мкФ на первой клемме 1-Wire — добавить в BOM
- [ ] **TODO**: фаза `STABILIZE` 15 мин между HEADS и BODY — обновить алгоритм 7.2
- [ ] **TODO**: timeout по dT/dt в фазе BODY (alert в Telegram) — обновить 7.2
- [ ] **TODO**: explicit reset ШИМ-параметров при смене фазы — обновить 7.3
- [ ] **TODO**: калибровка реального mL/min отбора в первой сессии — добавить в 13.3
- [ ] **TODO V1**: PZEM-004T контроль реальной мощности — добавить в Phase 6

### 12.10 Международный опыт и индустриальные практики

Дополнительная порция уроков, которая выходит за рамки русскоязычного форума.

#### Подделки Fotek SSR (КРИТИЧНО для пожарной безопасности)

Документировано ProtoSupplies, Hackaday, AussieHomeBrewer, HomeBrewTalk:

- **Поддельные Fotek SSR-25/40/80** массово продаются на AliExpress, eBay, Amazon Marketplace. Внутри корпуса с маркировкой «40 А» стоит реальный триак **BTA16-600 (16 А)** в ранних подделках, **позже урезали до 12 А**. У SSR-80 ситуация может быть ещё хуже.
- **Документированные пожары**: пользователи на homebrewtalk.com описывают, как SSR с лампочкой 100 Вт «адекватно работает», но при подключении ТЭНа 4–5 кВт корпус плавится, идёт дым, а в одном случае «если бы вышли из комнаты — сгорел бы дом». Hackaday в разделе Fail of the Week — несколько драматических разборов.
- **Внутри подделки**: PCB без минимальных зазоров между входной и выходной частью (риск пробоя), реальный триак в 2–3 раза меньше заявленного, термопаста между триаком и корпусом отсутствует или халтурная.
- **Как отличить подделку** (несколько признаков сразу — оригинал, один признак — почти наверняка фейк):
  - **Вес**: оригинальный Fotek SSR-40DA весит ~75 г, поддельный 30–45 г
  - **Шрифт на корпусе**: оригинал — чёткая лазерная гравировка с одинаковой глубиной, подделка — смазанная краска или мелкая печать
  - **Голограмма Fotek** на задней наклейке у оригинала (есть не у всех серий, но если совсем нет — подозрительно)
  - **Цена**: оригинальный Crydom D2440 у Mouser ≈ $25–35, оригинальный Fotek SSR-40DA $10–15 у официальных дилеров. SSR-40DA за $3 с AliExpress = почти гарантированно подделка
- **Наша рекомендация**:
  1. **Лучший вариант**: Crydom D2425 (24 А) или D2440 (40 А) у Mouser/Digi-Key/Farnell — оригинал, гарантия, ~$25–35
  2. **Бюджетный**: SSR-80DA «на нагрузку 22 А» (двойной запас даже от подделки), от поставщика с возвратом, обязательно Тест 5 (тепловой) с замером веса перед монтажом
  3. **Минимум**: НЕ ставить SSR-40 на нагрузку >15 А, даже если на корпусе написано «40 А»

Тепловой Тест 5 в Phase 3 — последняя линия защиты от подделки: подделка выйдет из теплового бюджета за первые 10–15 мин полной нагрузки и сгорит на тестовой лампе или испытательном ТЭНе, а не во время сессии с брагой.

#### Bang-bang vs PID для reflux column (см. 7.4)

Опыт англоязычных форумов: наивный PID(T_vapor) → reflux_water_flow ломает колонну (subcool trap). Правильный паттерн — **bang-bang клапан воды + ШИМ-отбор**, как реализовано в БКУ-07М и StillDragon-овском «still controller» из коробки. Cascade PID — только для тех, кто хочет «automatic ABV mode».

#### Архитектура «Pi + микроконтроллер с локальным safety» — проверенный паттерн

BrewPi — самая известная open-source платформа для пивоварения (с 2013 года). Архитектура:
- Arduino держит температурный контроль ферментера локально
- Raspberry Pi — UI, графики, конфиг, история
- Цитата с homebrewtalk.com: «если Pi падает прямо в середине ферментации, Arduino берёт управление, и контроль температуры продолжается»

Это ровно наша архитектура (ESP32 ↔ Pi). Подтверждение, что разделение «brain on Pi, safety on MCU» — рабочий промышленный паттерн для домашних процессов, а не наша выдумка.

#### Larry Athey's rpi-smart-still (github.com/larry-athey/rpi-smart-still)

Близкий по концепции проект (Pi + ESP32, контроль T500 и других колонн с дефлегматором). Уроки:

- Использует **сервоприводы на водяных вентилях** (proportional), не on/off — даёт более тонкий контроль ABV ценой механической сложности и точки отказа (сервопривод)
- Принципиально **требует ручную «training run»** перед автоматизацией: «still must be manually run and notes made». Нет универсального алгоритма, который работает на любом кубе из коробки.
- Явно отказались от ML/AI: «There is no AI in use here» — простая state machine на параметрах из калибровки
- **Не заменяет ручные cuts**: «It's upon the distiller themselves to take cuts because there is no way to make flavor determinations electronically» — наш план MVP с «авто-сердце» по T_head должен иметь явный override через UI

#### Коммерческие системы (iStill, GENIO) — что у них «фишкой»

Для понимания потолка автоматизации:

- **GENIO**: запатентованный алгоритм управления **игольчатым клапаном с шагом 0.0208 мм** даёт constant 96 % ABV выхода. Reflux ratio управляется не водой, а механической задвижкой на парном тракте.
- **iStill**: явное профилирование «recipe» с настройкой heads/hearts/tails cuts; интегрировано управление давлением (active column manager — ACM).
- **Чему учиться**: явное хранение «recipe» (профиля сессии) и сохранение фактических параметров успешной сессии как нового пресета — это та фича, которая делает «домашнюю» автоматику конкурентоспособной. У нас в плане она есть (SQLite пресеты), но стоит уделить внимание UX.

#### Индустриальные стандарты безопасности (TTB / OSHA / NFPA)

Применительно к коммерческим distilleries в США/UK — overkill для дома, но направление правильное:

- **TTB 27 CFR Part 19** — учёт алкоголя, не релевантно
- **OSHA 29 CFR 1910.106** — Flammable Liquids — релевантно: помещение должно быть вентилируемо так, чтобы концентрация паров спирта в воздухе была **<25 % LEL (LEL этанола ≈3.3 % об., значит <0.83 % об.)**
- **NFPA 70 / NEC Class I Division 1 или 2** — электрооборудование в опасной зоне должно быть искробезопасным (Ex-rated). Для дома: щит и реле физически вынести из «горячей зоны» (≥1.5 м от куба), кабели через гермоввод
- **Огнетушитель 40-B:C в радиусе 15 м** от точки операции — минимум для коммерции, дом: углекислотный или порошковый ≥2 кг в той же комнате
- **Газоанализатор на LEL** не обязателен **если** вентиляция достаточная (приток + вытяжка ≥10× объёма помещения в час). У нас на кухне эту цифру не достичь — рекомендуем либо вытяжку, либо опционально дешёвый MQ-3 (этанол) на ESP отдельным каналом GPIO для warning'а; не как safety-critical

#### Проблема «cycle in power input drops vapor out of suspension»

Из StillDragon: если резко включать/выключать ТЭН (а не плавно регулировать), при каждом провале мощности часть пара конденсируется обратно в куб, теряется фракция. Поэтому **slow-PWM с длинным периодом (2–5 сек)** лучше быстрого ШИМа или on/off — нагрев усредняется, поток пара стабилен.

- **Наш ответ**: уже заложено — ESPHome `slow_pwm` с периодом 5 сек.

#### Резюме новых уроков (что добавилось в чек-лист)

- [x] SSR — Crydom или проверенный Fotek с двойным запасом, никогда не SSR-40 на 22 А с AliExpress (BOM, 14)
- [x] PID на reflux — НЕ наивный, для MVP только bang-bang (7.4 переписан)
- [x] Slow-PWM ТЭНа с периодом 2–5 сек (уже было)
- [ ] **TODO**: «manual training run» как обязательный шаг перед первой автоматической сессией — добавить в 13.3
- [ ] **TODO V2**: рассмотреть сервопривод на дроссельном клапане отбора для constant-ABV режима (после первых 5–10 успешных сессий с bang-bang)
- [ ] **TODO опц.**: MQ-3 газоанализатор на отдельный GPIO для предупреждения о парах — Phase 6

### 12.11 Источники

Подобранные ветки и материалы (на момент мая 2026):

**Русскоязычные (homedistiller.ru):**

- [Автоматика — раздел форума](https://forum.homedistiller.ru/index.php?board=12.0)
- [Автоматика с веб-управлением на ESP32 + PZEM (315674)](https://forum.homedistiller.ru/index.php?topic=315674.0) — boot loop, WiFi
- [Проект Samovar (367128)](https://forum.homedistiller.ru/index.php?topic=367128.0) — контроль расхождения мощности через PZEM
- [Автоматика HelloDistiller (105339)](https://forum.homedistiller.ru/index.php?topic=105339.0) — стабилизация перед отбором тела
- [Автоматизация колонны на ESPHome+HA (396636)](https://forum.homedistiller.ru/index.php?topic=396636.0) — близкий по архитектуре проект
- [Ошибки при освоении автоматики БКУ-099 (307650)](https://forum.homedistiller.ru/index.php?topic=307650.0)
- [Автоматика БКУ-097 + клапан голов (308098)](https://forum.homedistiller.ru/index.php?topic=308098.0)
- [Мой алгоритм автоматизации колонны (139454)](https://forum.homedistiller.ru/index.php?topic=139454.0) — критика чужого алгоритма
- [Вопрос о надёжности DS18B20 (5378)](https://forum.homedistiller.ru/index.php?topic=5378.0)
- [Глючит датчик DS18B20 (50867)](https://forum.homedistiller.ru/index.php?topic=50867.0) — экран, ферриты, конденсатор 50 мкФ
- [Провалы DS18B20 (375)](https://forum.homedistiller.ru/index.php?topic=375.0)
- [Захлёбывание колонны (365617)](https://forum.homedistiller.ru/index.php?topic=365617.0)
- [Скачки давления при ректификации (50922)](https://forum.homedistiller.ru/index.php?topic=50922.0)
- [Правильное место установки термометра (15294)](https://forum.homedistiller.ru/index.php?topic=15294.0)
- [Клапан старт-стопа: внимание, засада (71010)](https://forum.homedistiller.ru/index.php?topic=71010.0) — силиконовые прокладки
- [Клапан старт-стопа, автоматика (35787)](https://forum.homedistiller.ru/index.php?topic=35787.0)
- [Правила техники безопасности (41584)](https://forum.homedistiller.ru/index.php?topic=41584.0)
- [ЧаВО по безопасности (статья)](https://www.homedistiller.ru/faq-bezopasnost.htm)

**Англоязычные форумы и блоги:**

- [Home Distiller forum (homedistiller.org)](https://homedistiller.org/forum/) — крупнейший англоязычный
- [«Me running my CM reflux still by using a PID controller» (homedistiller.org/77285)](https://homedistiller.org/forum/viewtopic.php?t=77285) — глубокая дискуссия про PID на reflux
- [«PID on reflux still not chasing temperature» (homedistiller.org/77296)](https://homedistiller.org/forum/viewtopic.php?t=77296) — конкретный кейс subcool trap
- [StillDragon forum](https://www.stilldragon.org/discussion/1450/my-pid-experiments) — PID experiments thread
- [«Managing a Reflux Still Column» — StillDragon blog](https://stilldragon.com/blog/managing-a-reflux-still-column/) — теория управления refluxen
- [Aussie Distiller forum — PID Controller](https://www.aussiedistiller.com.au/viewtopic.php?f=24&t=6158)

**Counterfeit SSR — пожарная безопасность:**

- [ProtoSupplies — Inferior Counterfeit FOTEK SSR-25](https://protosupplies.com/inferior-counterfeit-fotek-ssr-25-solid-state-relays-on-the-market/)
- [Hackaday — Fail of the Week: SSR Fails Spectacularly](https://hackaday.com/2018/08/24/fail-of-the-week-solid-state-relay-fails-spectacularly/)
- [Aussie Home Brewer — COUNTERFEIT SSR WARNING](https://aussiehomebrewer.com/threads/counterfeit-ssr-warning-check-your-gear.90323/)
- [Epic Beard Quest — How to destroy a fake FOTEK SSR](http://epicbeardquest.blogspot.com/2014/10/how-to-destroy-fake-fotek-ssr.html)
- [Home Brew Talk — Fotek SSRs are not THAT bad](https://homebrewtalk.com/threads/fotek-ssrs-are-not-that-bad.570792/) — противоположная точка зрения с оговорками

**Open-source проекты-аналоги:**

- [larry-athey/rpi-smart-still (GitHub)](https://github.com/larry-athey/rpi-smart-still) — Pi+ESP32, сервоприводы на воде
- [Open Source Distilling](https://opensourcedistilling.com/) — Python на Pi, 2" Nixon-Stone reflux
- [BrewPi (homebrewersassociation статья)](https://homebrewersassociation.org/how-to-brew/brew-pi-the-future-of-homebrew-fermentation/) — архитектурный образец Pi+Arduino
- [CraftBeerPi (opensource.com)](https://opensource.com/article/17/7/brewing-beer-python-and-raspberry-pi)

**Индустриальные стандарты и теория:**

- [Control Engineering — Distillation Columns Internal Reflux Control](https://www.controleng.com/distillation-columns-internal-reflux-control/) — про cascade
- [Bluefield Process Safety — Ethanol limits](https://bluefieldsafety.com/2025/08/ethanol-what-are-the-limits/) — LEL, концентрации
- [OSHA Technical Manual Section IV Ch. 5](https://www.osha.gov/otm/section-4-safety-hazards/chapter-5)
- [Distiller Magazine — Electric Sombrero of Death](https://distilling.com/distillermagazine/electric-sombrero-of-death/) — про электрификацию дистилляторов

**Немецкоязычные:**

- [Hobbybrauer.de — Raspberry PI Steuerung](https://hobbybrauer.de/forum/viewtopic.php?t=3959) — длинная ветка про PT100 + Arduino + Pi для пивоварения, подходы переиспользуемы

**Коммерческие системы (для понимания «потолка»):**

- [iStill (istill.com)](https://istill.com/distillery-equipment/) — recipe profiles, ABV control, ACM
- [GENIO Stills (gstill.co.uk)](https://www.gstill.co.uk/why-choose-genio) — игольчатый клапан 0.0208 мм шаг

## 13. Dry-тесты — план

Цель — проверить всю логику системы без алкоголя и без реальной дистилляции. Делаем в собранном щите. Лампа вместо ТЭНа, светодиоды/индикаторы вместо клапанов, кружка с водой для прогрева датчиков. **Всё, что описано ниже, выполняется ДО первого боевого запуска.**

### 13.1 Подмены компонентов

| Боевой компонент | Чем заменить для dry test | Зачем |
|---|---|---|
| ТЭН 3–5 кВт | **Лампа накаливания 60–100 Вт** (Е27 в патроне с проводами и вилкой) | Видимая нагрузка, минимум тока (≤0.5 А), не нужен большой радиатор на SSR. Накал ламы видно глазом ⇒ ШИМ проверяется визуально |
| Соленоид воды 230 В | **Лампа-индикатор 230 В неоновая** (зелёная) или сетевой LED 5 В через адаптер | Видимая индикация открытия/закрытия |
| Клапан отбора БКУ | **Лампа-индикатор 230 В неоновая** (красная) | То же |
| DS18B20 (×4–6) | Сами боевые датчики | Опускаем по одному в чашку с горячей/холодной водой, имитируем рост T |
| Куб с ТЭНом | **Электрочайник + датчик внутри** (изолированно, через крышку) | Чайник плавно греет воду — идеальный имитатор ТЭНа для отладки переходов состояний |
| BME280 | Сам боевой | Просто читаем атмосферу комнаты |

Стоимость доп. железа для dry test: ~$10. Лампа в патроне + 2 неонки + чайник, который и так есть.

### 13.2 Последовательность тестов

#### Тест 1 — Базовые I/O (Phase 1)

1. Прошить ESPHome на ESP32, подключить DS18B20 + BME280.
2. Поднять `controller/` на Pi (или ноуте в режиме разработки), подключиться к ESP через aioesphomeapi.
3. В веб-UI на `http://still.local:8000/tests` (экран «Tests») убедиться: все датчики читаются, значения обновляются раз в 1–5 сек.
4. Поднести зажигалку к гильзе DS18B20 (НЕ на пластик!) на 5 сек — T должна вырасти на 10–20 °C, потом упасть.
5. Подышать на BME280 — влажность подскочит на 10–20 %.

**Прохождение**: все 4 датчика T и BME280 видны в UI, реагируют на тепло.

#### Тест 2 — Логические выходы (Phase 2)

1. Собрать щит до уровня «ESP + screw shield + SSR + реле», но **без подключения к 230 В сети**.
2. На вход 230 В подать через лампу-индикатор 60 Вт (т.е. в разрыв линии), чтобы видеть «есть/нет напряжение на нагрузке».
3. Из UI экрана «Tests» нажать `[TEST 5s]` на каждом выходе → соответствующая лампа-индикатор должна загораться на 5 сек.
4. Для ТЭН-выхода: задать ШИМ 50 % на 30 сек → лампа на выходе SSR должна **мигать** с периодом 2–5 сек, ~равные интервалы on/off.
5. Проверить кнопку BEEP → пищит.

**Прохождение**: каждый выход срабатывает по команде, ШИМ виден как чёткое мигание лампы.

#### Тест 3 — Safety chain (Phase 3, до первого боевого)

1. Собрать щит полностью, **но вместо ТЭНа — лампа накаливания 100 Вт в патроне** (через клеммы ТЭНа).
2. Подать 230 В на щит, включить контактор через UI (Tests → ESP→enable → ON).
3. На экране Run задать heater = 100 % → лампа загорается на полную яркость.
4. **Тест E-stop**: нажать грибок → лампа **гаснет МГНОВЕННО** (<1 сек, по сути 1 цикл AC ≈20 мс). Отпустить грибок, заново активировать enable из UI → лампа снова горит.
5. **Тест биметалла**: прогреть KSD9700 феном/зажигалкой до >110 °C → лампа гаснет. Остудить → размкнутый контакт обратно замкнётся, но enable-relay уже отжат safety-логикой ESP, надо включить заново из UI.
6. **Тест термопреда**: **НЕ ТЕСТИРОВАТЬ грубо** — он одноразовый. Просто прозвонить мультиметром в холодном состоянии (должен звонить, R<1 Ом).
7. **Тест watchdog (потеря связи)**: отключить Wi-Fi на роутере или выключить Pi на 60 сек → через ≤30 сек ESP должен сам отжать enable-relay → лампа гаснет, пищалка пищит. Включить обратно, в UI должно показать аварию в логе.

**Прохождение**: каждое размыкание гасит лампу за <1 сек, watchdog срабатывает за ≤30 сек.

#### Тест 4 — Slow PWM ТЭНа под нагрузкой (Phase 3)

1. С лампой 100 Вт вместо ТЭНа задать из UI мощность:
   - 25 % → лампа коротко вспыхивает раз в ~2–3 сек, основную часть периода погашена
   - 50 % → симметричные on/off с равными интервалами
   - 75 % → длинные импульсы on, короткие off
   - 100 % → постоянное свечение
   - 0 % → погасла
2. Замерить мультиметром (True-RMS, если есть) средний ток через лампу при 50 % → должен быть ~½ номинального.

**Прохождение**: ШИМ визуально и метрологически работает.

#### Тест 5 — Тепловой тест SSR (Phase 3, перед боем)

1. Подключить **реальный ТЭН**, налить полный куб **воды** (без браги!).
2. В UI задать мощность 100 %, контактор ON.
3. Каждые 5 минут замерять температуру корпуса SSR ИК-термометром или контактным термопаро-мультиметром.
4. Через 15 минут полной нагрузки **температура SSR должна стабилизироваться ≤ 80 °C** при окружающей 25 °C (т.е. ΔT ≤ 55 °C).
5. Если SSR > 80 °C через 15 минут — **выключить, добавить радиатор крупнее или вентилятор**. Не пытайтесь работать на пределе.

**Прохождение**: T корпуса SSR ≤ 80 °C на полной мощности в течение часа.

#### Тест 6 — Алгоритм потстила «на воде» (Phase 4)

1. Реальный ТЭН, реальные клапаны (вода в дефлегматор течёт реально), куб с **водой** (без браги).
2. В UI запустить режим «Потстил» с параметрами как для дистилляции (T_start=80, T_body=85, T_stop=99).
3. Проверить, что система проходит фазы:
   - HEAT_UP: heater=100 %, ждём пока T_kub дойдёт до 80
   - DISTILLING: heater=p_work, клапан воды открыт (вода поступает), клапан отбора открыт (но сольёт просто горячую воду)
   - SHUTDOWN: при T_kub > 99 → всё гаснет, пищит, сессия закрыта в БД
4. Записать в `docs/calibration.md` фактические времена и поведение для будущей подстройки.

**Прохождение**: все фазы пройдены автоматически, переходы корректные, finалом — safe state.

#### Тест 7 — Симуляция аварий в работе (Phase 4)

С работающим режимом потстила на воде:
1. **Выдернуть провод DS18B20 куба** → через 5 сек (3 чтения подряд с ошибкой) ESP уходит в safe state, в UI авария «Sensor T_kub lost», пищалка. Контактор отжат.
2. **Закоротить провод DS18B20 куба** (фаза → DQ на 1 сек) → 1-Wire CRC error → ESP игнорирует на 5 сек, потом safe state.
3. **Закрыть кран водопроводной воды** в дефлегматор → нет потока, но мы не имеем расходомера ⇒ обнаружим только косвенно: T выхода дефлегматора растёт ненормально. Лог: добавляем мягкую проверку ΔT(out-in) > 50 °C → warning в UI (не авария).
4. **Отключить Pi от сети** → через ≤30 сек ESP watchdog → safe state.

**Прохождение**: все аварии корректно регистрируются и приводят в safe state.

### 13.3 Чек-лист перед первым «боевым» запуском с брагой

- [ ] Все 7 dry-тестов пройдены, лог в `docs/dry_test_results.md` с датой и фактическими цифрами
- [ ] Силовое заземление проверено: R < 1 Ом между корпусом ТЭНа/куба и PE-шиной щита
- [ ] УЗО проверено кнопкой TEST → размыкается за <30 мс
- [ ] Радиатор SSR ≤ 80 °C после 60 минут полной нагрузки
- [ ] Все винтовые клеммы протянуты после первого включения (медь «садится» на ~5 % за первый прогрев)
- [ ] Снаббер RC установлен на клеммах ТЭНа
- [ ] Все DS18B20 откалиброваны в кипящей воде и ледяной воде, offset сохранён в БД
- [ ] BME280 показывает разумное давление (1000–1030 hPa)
- [ ] Watchdog работает: тест отключения Pi на 60 сек
- [ ] Telegram-уведомления (если настроены) приходят на «start», «stop», «авария»
- [ ] Первая сессия записана в БД с полным графиком
- [ ] **Огнетушитель углекислотный или порошковый ≥2 кг рядом с установкой**
- [ ] **Вентиляция в помещении** (открытое окно или вытяжка) — пары спирта горючи
- [ ] **Никого не оставляйте установку без присмотра в первые 5 сессий**

## 14. Финальный shopping list (то, что заказать сейчас)

**Главное правило при заказе: все модули должны быть В СБОРЕ, не «kit». Ищите фото готовой платы с распаянными компонентами и винтовыми клеммами.**

### Хост-машина (Pi + дисплей)

- [ ] **Raspberry Pi 4 Model B, 4 GB** + БП 5В 3А USB-C официальный + пассивный радиатор-корпус (FLIRC или Argon NEO) — 1 шт
- [ ] **USB 3.0 SSD 64–128 GB** (Samsung T7 / Kingston DT80 / Crucial X6) + USB-A ↔ USB-C кабель — 1 шт
- [ ] **microSD 32 GB Class 10, A2** (recovery + первая установка) — 1 шт
- [ ] **Raspberry Pi 7" Touch Display 2** (DSI) или эквивалент HDMI+USB-touch 7"/10" с подтверждённой Linux-поддержкой — 1 шт
- [ ] **Шлейф DSI и кабели питания дисплея** (обычно в комплекте с официальным дисплеем) — комплект
- [ ] **Крепление дисплея на дверцу щита** или отдельная VESA-рамка — 1 шт
- [ ] **Короткий HDMI-кабель + клавиатура USB** для первой настройки (можно одолжить) — 1 раз

### С AliExpress / Amazon (электроника управления)

- [ ] **ESP32-S3-DevKitC-1** (любая память), с заводски припаянными штырьками — 1 шт
- [ ] **Screw terminal expansion board под ESP32-S3-DevKitC-1 (44-pin)** — 1 шт. Запрос: `ESP32-S3 DevKitC-1 expansion board screw terminal` или `ESP32-S3 N16R8 screw shield`. **Сверьте ширину/число пинов с фото DevKit** — это критично, не путать с 38-pin от классики.
- [ ] **DS18B20 IP67 в гильзе, 1м кабель** — добрать до 6 шт
- [ ] **BME280 breakout, с распаянными штырьками** (`BME280 module pre-soldered 4 pin`), 3.3В I²C — 1 шт
- [ ] **Omron G3MB-202P breakout ASSEMBLED** (`G3MB-202P module 5V assembled`) — 3 шт (клапан отбора, клапан воды, +запас)
- [ ] **5В релейный модуль 1ch**, opto-isolated, low-level 3.3V trigger, в сборе — 1 шт (на enable-контактор)
- [ ] **Соленоидный клапан 1/2" NC 230В AC** для холодной воды — 1 шт. Проверьте, что катушка ~230В AC, а не 12/24В DC.
- [ ] **Резисторы 4.7 кΩ** 1/4 Вт с длинными выводами (под винт.клеммы) — 5 шт
- [ ] **Снаббер RC** (плёнка 0.1 мкФ X2 250В + резистор 100 Ом 2Вт), отдельные компоненты или готовый модуль — 2 комплекта
- [ ] **Активная пищалка 3.3–5В** с проводами — 1 шт
- [ ] **Кнопка E-stop грибок** NC, фиксируемая, 22 мм, 16А — 1 шт
- [ ] **Биметаллический термостат KSD9700 110°C NC** — 2 шт
- [ ] **Термопредохранитель** 130°C или 140°C NC, керамический — 2 шт
- [ ] **DuPont F-F джамперы**, набор 40 шт. × 20 см — 1 набор

### В электротоварах (или АББ/Шнайдер дилер)

- [ ] **Mean Well IRM-15-5** (5В 3А DIN) — 1 шт
- [ ] **Силовой контактор модульный, 230В AC катушка, 25А, 2NO** (ABB ESB25-40N-06 / Schneider iCT25 / IEK КМИ-22510) — 1 шт
- [ ] **Автомат C25** (или C16 если ТЭН ≤3 кВт) — 1 шт
- [ ] **УЗО 30 мА, 25А** — 1 шт (если ещё нет на отдельной линии)
- [ ] **SSR на ТЭН** — **рекомендуется Crydom D2425/D2440 у Mouser/Digi-Key (оригинал, ~$25–35)**. Бюджетный вариант: Fotek SSR-80DA **только** на нагрузку 22 А (двойной запас от номинала) и **только** после проверки веса (оригинал ≥60 г, подделка 30–40 г) и тепла на Тесте 5. **НЕ покупать SSR-40 на 22 А с AliExpress** — высокая вероятность подделки с реальным 12-А триаком. См. секцию 12.10
- [ ] **Радиатор 150×100×40 мм** (для 5 кВт) или **120×80×30 мм** (для 3 кВт) + термопаста КПТ-8 или MX-4 — 1 шт
- [ ] **MOV (варистор) 275 В AC** на катушку контактора — 1 шт (опц., снижает помехи при отпускании)
- [ ] **Предохранитель керамический 25А** + DIN-держатель — 1 шт
- [ ] **Кабель ВВГнг 3×4** (или 3×2.5 если ≤3 кВт) — по длине
- [ ] **DIN-щит наружный, IP54, на 12–18 модулей** — 1 шт
- [ ] **Wago 221 ассортимент** (221-412, 221-413, 221-415) — набор 40–60 шт
- [ ] **DIN-клеммы пружинные** для распределения внутри щита — 10 шт
- [ ] **Гофра кабельная** 16/20 мм — по необходимости

### Для dry-тестов (можно с подручного)

- [ ] **Лампа накаливания 100 Вт E27** + патрон с проводами и вилкой — 1 шт (вместо ТЭНа для Тестов 3, 4)
- [ ] **Неоновая лампа-индикатор 230 В** в корпусе, с проводами — 2 шт (вместо клапанов для Тестов 1, 2)
- [ ] **ИК-термометр** (любой бытовой $20–30) — 1 шт (для Теста 5: замер T корпуса SSR)
- [ ] **Опц. вентилятор 40×40 мм 12 В** + блок 12 В DC + термостат KSD9700 75°C NO — если радиатор SSR не справляется

### Инструменты (купить один раз)

- [ ] **Кримпер для НШВИ** (Knipex 97 53 04 / Engineer PA-21 / квадратный AliExpress) — 1 шт
- [ ] **Набор гильз НШВИ** 0.5–4.0 мм² — 1 набор
- [ ] **Автоматический стриппер** проводов — 1 шт
- [ ] **Мультиметр** с прозвонкой — 1 шт (если ещё нет)
- [ ] **Отвёртки** PH1, PZ1, плоская 2.5 мм — комплект (если ещё нет)

### Из подручного / уже есть

- ✅ DS18B20 от БКУ-07М (сколько есть)
- ✅ Клапан отбора 230В NC от БКУ
- ✅ Куб с ТЭНом, царга, дефлегматор

## 15. Референсы

- [BKU-07M описание](https://pivoperm.ru/magazin/product/983-avtomatika-bky-07m.html) — что мы повторяем
- [LKosoj/Samovar](https://github.com/LKosoj/Samovar) — open-source аналог на ESP, полезен как референс алгоритмов
- [RectifyPlugin для CraftBeerPi](https://github.com/saniaxxx/RectifyPlugin) — пример старт-стоп алгоритма
- [Обсуждение CraftBeerPi для ректификации (homedistiller.ru)](https://forum.homedistiller.ru/index.php?topic=308749.0)
- [ESPHome `ac_dimmer` компонент](https://esphome.io/components/output/ac_dimmer.html)
- [Home Assistant — создание custom integration](https://developers.home-assistant.io/docs/creating_integration_manifest)
